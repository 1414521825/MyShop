

### 鸿蒙官方MVVM（V2）架构AI自检约束清单

请按照以下规则对项目代码进行检查，若发现违反以下约束的代码，请标记为“不符合鸿蒙官方MVVM规范”，并给出修改建议：

#### 一、 架构分层职责约束

1. View（视图层）约束

   ：

   - **职责单一**：只能负责界面展示和与用户的交互，**绝对不能包含任何业务逻辑**。
   - **数据获取**：只能通过绑定ViewModel层提供的数据来动态更新UI，不能直接发起网络请求或直接读写数据库。

2. ViewModel（逻辑层）约束

   ：

   - **桥梁作用**：必须作为连接Model和View的桥梁，负责管理UI状态和交互逻辑。
   - **数据监控**：需通过V2状态装饰器监控Model数据的变化，并将处理后的数据同步给View层。
   - **逻辑处理**：需处理来自View层的用户交互事件，并将其转换为对Model层的数据操作。

3. Model（数据层）约束

   ：

   - **纯数据与逻辑**：负责存储和管理应用数据及核心业务逻辑，通常与后端或数据存储交互。
   - **界面隔离**：**绝对不能直接与用户界面（UI）交互**，不能引入任何UI库或操作UI刷新。

#### 二、 跨层与组件通信原则（核心红线）

1. 不可跨层访问

   ：

   - View层**禁止**直接调用或修改Model层的数据，必须通过ViewModel提供的方法进行调用。
   - Model层**禁止**直接操作UI，只能通过数据更新通知ViewModel层。

2. 下层不可访问上层数据

   ：

   - 数据流必须是单向或通过通知模式更新。在业务逻辑中，下层（如Model）不可直接获取上层（如ViewModel/View）的数据或依赖其界面上的状态值。

3. 非父子组件不可直接访问

   ：

   - **禁止**组件直接访问父组件或兄弟组件的内部状态。
   - 必须通过向子组件传参（向下传递），或通过事件/发布订阅能力（向上/向外传递）来完成组件间的解耦。

#### 三、 鸿蒙V2状态管理规范（代码级自检）

代码中状态管理必须优先使用V2版本的装饰器与工具：

1. **组件内部状态**：只能使用 `@Local` 装饰器管理组件内部的独立状态。

2. 父子组件数据传递

   ：

   - 子组件接收外部输入必须使用 `@Param`（默认只读）；若需在本地修改初始值，需配合 `@Once` 使用。
   - 子组件向父组件传递数据或触发行为，必须通过触发 `@Event` 装饰的事件完成。

3. 复杂类与深层嵌套数据

   ：

   - 必须将多层嵌套或继承的数据模型定义为类，并使用 `@ObservedV2` 装饰该类。
   - 类中需要触发UI刷新的具体属性，必须明确使用 `@Trace` 标记。

4. 状态监听与派生计算

   ：

   - 若需深度监听某个状态变量的变化并触发副作用逻辑，应使用 `@Monitor` 装饰器。
   - 依赖其他状态变量派生出的值，必须使用 `@Computed` 装饰器修饰 getter 方法，避免重复计算耗费性能。

5. 全局与持久化状态

   ：

   - 跨页面或跨 UIAbility 的运行时状态共享，必须使用 `AppStorageV2`。
   - 需要落盘保存（关闭应用不丢失）的UI状态数据，必须使用 `PersistenceV2`。

6. UI渲染性能优化

   ：

   - 结构相同的子组件循环渲染，必须优先使用 `Repeat` 替代 `ForEach`，并根据数据量正确选择懒加载或非懒加载模式。
   - 页面中重复的UI元素，应使用 `@Builder` 抽象为独立的构建方法进行复用。

#### 四、 视图层（View）组件分类自检

AI应检查 View 层的组件是否被合理划分为以下三类，并符合各自的特征：

1. **页面组件（Page）**：提供整体页面布局，负责多页面之间的跳转、前后台事件等页面级能力。
2. **业务组件**：与当前项目业务强相关，**允许引用本项目的ViewModel**。如果没有对应的ViewModel，该组件无法独立运行。
3. **共享组件（通用组件）**：可跨项目使用的通用UI，**禁止包含或引用任何ViewModel层的数据**，所有需要的数据和事件交互必须完全通过外部参数（如 `@Param` 和 `@Event`）传入。